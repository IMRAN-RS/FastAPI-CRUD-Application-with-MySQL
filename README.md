# FastAPI-CRUD-Application-with-MySQL

📋 Project Overview

This project is a simple CRUD (Create, Read, Update, Delete) REST API built with FastAPI and connected to a MySQL database.
It allows you to perform operations like creating, reading, updating, and deleting employee records.


🚀 Features

Create (POST) a new employee
Read (GET) employee details
Update (PUT) an existing employee
Delete (DELETE) an employee
Connected to MySQL Workbench for automatic data storage

🛠️ Tech Stack

Python 3.10+
FastAPI
Uvicorn (for running the server)
SQLAlchemy (ORM)
MySQL Workbench / MySQL Server
Pydantic (for data validation)

